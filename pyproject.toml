[tool.pytest.ini_options]
testpaths    = ['tests']
python_files = ['test_*.py']
addopts      = '-v --tb=short --strict-markers'
pythonpath   = ['.']
markers = ['datos: checks de calidad sobre el dataset real']
